# Copy to cloudflared.local.yml (ignored by Git) and replace placeholders.
tunnel: YOUR-TUNNEL-UUID
credentials-file: C:/Users/YOUR_USER/.cloudflared/YOUR-TUNNEL-UUID.json
ingress:
  - hostname: face.example.com
    service: http://127.0.0.1:8767
  - service: http_status:404
